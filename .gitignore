# прогоны пробника и рабочие каталоги документов — результат, не исходник
probe-runs/
docs-runs/

__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# машинное: пути, UUID профиля шлюза — не для публичного репозитория
.claude/settings.local.json
# полные эталоны — клиентские документы; в репозитории только скелеты (SPEC Р8)
exemplars.local.yaml
logs/
